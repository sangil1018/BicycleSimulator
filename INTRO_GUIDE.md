# 📽️ 인트로 구성 가이드 (Intro Configuration Guide) - v1.0

이 문서는 레벨 시작 시 재생되는 **인트로(가이드 영상)** 시스템의 구성과 제어 로직을 설명합니다.

---

## 1. 개요
모든 레벨 씬(Level1, Level2 등)은 시작 시 사용자에게 조작법 및 주의사항을 안내하는 가이드 영상을 재생합니다. 이 과정은 `IntroManager.cs`에 의해 관리됩니다.

---

## 2. 주요 기능 및 로직

### 2.1. 비디오 로딩 및 재생 (Prepare & Play)
*   **준비 단계**: 씬이 로드되면 `introVideoPlayer.Prepare()`를 호출하여 영상을 백그라운드에서 로드합니다.
*   **재생 시점**: `isPrepared`가 `true`가 되는 시점에 실제 재생을 시작하며, 이때부터 모든 타이머가 작동합니다. 이는 하드웨어 사양에 따른 로딩 지연이 발생해도 영상과 UI 타이밍이 어긋나지 않게 하기 위함입니다.

### 2.2. 인트로 시퀀스 타이밍
*   **전체 지속 시간**: 기본 20초 (`introDuration`).
*   **트랜지션 트리거**: 종료 2초 전(18초 지점, `transitionStartTime`)에 `transitionUI`를 활성화하여 자연스럽게 본 게임 화면으로 전환을 준비합니다.

---

## 3. 조작 및 인터랙션
인트로 재생 중 사용자는 다음 버튼을 통해 상태를 변경할 수 있습니다.

*   **O 버튼 (건너뛰기)**:
    *   영상 재생을 중단하고 즉시 트랜지션 애니메이션을 실행하며 본 게임 대기 상태(`GameReady`)로 넘어갑니다.
*   **X 버튼 (홈으로)**:
    *   인트로를 취소하고 즉시 `Home` 씬으로 복귀합니다.

---

## 4. 인스펙터 설정 가이드 (IntroManager)
*   **VideoPlayer**: 해당 레벨에 맞는 MP4 가이드 영상이 할당되어야 합니다.
*   **Reaction Objects**: O/X 버튼 입력 시 사용자에게 피드백을 줄 하이라이트 오브젝트들을 할당합니다.
*   **Transition UI**: 인트로 종료 시 재생될 애니메이션 UI를 할당합니다.

---

## 5. 주의 사항
*   **입력 연동**: O/X 버튼 입력은 UI 버튼 오브젝트가 `IntroManager.HandleSkip()`(건너뛰기) / `HandleHome()`(홈)을 호출하는 방식으로 연결됩니다. `IntroManager`는 `InputManager` 이벤트를 직접 구독하지 않습니다.
*   **비디오 준비**: `Prepare()` 후 최대 5초까지 `isPrepared`를 기다렸다가 재생하며, 시간 초과 시 영상 없이 인트로 타이머를 진행합니다.
*   **GameManager 연동**: 인트로가 완료되면 `GameManager.Instance.OnIntroFinished()`를 호출하여 전체 게임 상태를 `GameReady`로 전환합니다.
